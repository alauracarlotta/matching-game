import './style.css'

const PlayerScore = (player) => {
    return /* html */ `
        <div class="press-start-2p-regular">
            Player ${player}
        </div>
    `
};

export default PlayerScore;
