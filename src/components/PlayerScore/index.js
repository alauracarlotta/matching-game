import './style.css'

const PlayerScore = (player) => {
    return /* html */ `
        <div class="name-player press-start-2p-regular">
            Player ${player}
        </div>
    `
};

export default PlayerScore;
