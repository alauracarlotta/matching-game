import './style.css'

const NamePlayer = (player) => {
    return /* html */ `
        <div class="press-start-2p-regular">
            Player ${player}
        </div>
    `
};

export default NamePlayer;
